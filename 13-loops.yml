- name: Demo on loops
  hosts: localhost
  tasks:
  - name: print the names
    ansible.builtin.debug:
       msg: "HELLO {{item}}"
    loop:
       - Nandu
       - surya
       - shiva
       - Abhi
